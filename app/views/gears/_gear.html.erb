<div id="<%= dom_id gear %>">
  <h2 class="mb-2"><%= gear.name %></h2>
  <p>
    <strong>Description:</strong>
    <%= gear.description %>
  </p>

  <p>
    <strong>Category:</strong>
    <%= gear.category %>
  </p>

  <p>
    <strong>Localisation:</strong>
    <%= gear.address %>
  </p>
</div>
